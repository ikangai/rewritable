{
  "name": "rewritable",
  "version": "0.15.0",
  "description": "CLI for re-writeable: emit and import single-file rwa documents.",
  "type": "module",
  "bin": {
    "rwa": "bin/rwa.mjs"
  },
  "files": [
    "bin/",
    "src/",
    "seeds/",
    "README.md"
  ],
  "engines": {
    "node": ">=20.16.0"
  },
  "dependencies": {
    "mammoth": "1.11.0",
    "marked": "^14.1.0",
    "papaparse": "^5.4.1",
    "pdfjs-dist": "5.4.149"
  },
  "scripts": {
    "prepublishOnly": "mkdir -p seeds && cp ../seeds/rewritable.html seeds/rewritable.html && cmp ../benchmark/oracles/dsl-compiler.mjs src/dsl-compiler.mjs && cp ../benchmark/oracles/dsl-compiler.mjs src/dsl-compiler.mjs"
  }
}
