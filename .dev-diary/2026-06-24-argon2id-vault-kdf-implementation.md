Tackled I9, the Argon2id vault KDF. Started by reviewing re-write-able-actions-spec-v0.9-open-items.md and inspecting seeds/rewritable.html for existing CSP constraints. The previous plan to use WASM would have required adding wasm-unsafe-eval to the script-src, which violates invariants 26 and 44 regarding a frozen CSP. I pivoted to a pure-JS implementation using @noble/hashes@latest. After installing it in a scratchpad and verifying RFC 9106 vectors with a node script, I wrote tools/vendor-argon2.mjs to bundle the necessary logic into an ARGON2_SRC block. I then injected this block into seeds/rewritable.html. Testing was difficult; running node tests/vault-kdf.mjs initially failed because the test environment lacked the necessary context for the new module. I had to update tests/lib/argon2-fallback.mjs and add shims to tests/vault.mjs and tests/vault-export.mjs to ensure the KDF worked within the jsdom-based test runner. After these changes, I ran a full loop over all tests/*.mjs files, confirming that vault-export.mjs and csp-boot.mjs remained green. Finally, I updated CLAUDE.md, the open items spec, and the project memory to reflect that I9 is complete.

A plan that breaks invariants is not a solution.
