I suspected the `authoring-rewritables` skill was running an outdated version of the CLI, so I started by comparing the modules in `~/.claude/skills/authoring-rewritables/src` against my local `cli/src`. Using a loop with `cmp -s`, I identified that several files—specifically `edit.mjs`, `apply-edits.mjs`, `identity.mjs`, and `skill-manifest.mjs`—had drifted from the current source. I updated the skill by copying the new modules and the `rewritable.html` seed into the skill directory. To ensure the re-vendoring didn't break the tool, I ran a smoke test with `rwa-lite.mjs`, using it to create a temporary file, inspect its JSON structure via `doc`, and execute an edit using a versioned envelope. The edit successfully updated the header as expected. Finally, I updated `VENDORED.md`, the skill's reference memory, and `MEMORY.md` to reflect the new version at commit `aefd0ab`.

Verification is the only way to trust a manual copy-paste operation.
